{
  "name": "thegraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ armughanahmed/thegraph",
    "create-local": "graph create --node http://localhost:8020/ armughanahmed/thegraph",
    "remove-local": "graph remove --node http://localhost:8020/ armughanahmed/thegraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 armughanahmed/thegraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  }
}
